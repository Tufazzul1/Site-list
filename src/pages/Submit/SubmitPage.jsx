import Form from "../../components/Form";

const SubmitPage = () => {
    return (
        <div className="h-full">
            <Form isUpdate={false} />
        </div>
    );
};

export default SubmitPage;
